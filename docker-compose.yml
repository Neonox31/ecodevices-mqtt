version: '2'
services:
  goapp:
    image: golang
    restart: always
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /srv/docker/go-ecodevices-mqtt:/config
    command: bash -c "go get github.com/neonox31/go-ecodevices-mqtt && exec go-ecodevices-mqtt --config /config/app.toml"